<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.ui.view" id="view_helpnet_tag_form">
        <field name="name">helpnet.tag.form</field>
        <field name="model">helpnet.tag</field>
        <field name="arch" type="xml">
            <form>
            <group>
                <field name="name"/>
                <field name="description"/>
            </group>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="view_helpnet_tag_tree">
        <field name="name">helpnet.tag.tree</field>
        <field name="model">helpnet.tag</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_helpnet_tag">
        <field name="name">Tag</field>
        <field name="res_model">helpnet.tag</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>